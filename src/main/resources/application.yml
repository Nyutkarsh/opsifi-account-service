server:
  port: 9090

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/onboarding_db
    username: postgres
    password: Postgres@07
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
